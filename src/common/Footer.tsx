import styled from 'styled-components';

function Footer() {
  return (
    <Wrapper>
      <span>CUBOX</span>
    </Wrapper>
  );
}

const Wrapper = styled.footer`
  height: 6.25rem;
  text-align: center;
`;

export default Footer;
